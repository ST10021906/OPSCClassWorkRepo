<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Donation10" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/blue</item>
        <item name="actionBarItemBackground">@color/blue</item>
        <item name="buttonTint">@color/blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:foregroundTint">@color/white</item>
        <item name="android:progressTint">@color/blue</item>
    </style>
</resources>